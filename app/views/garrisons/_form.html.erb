<%= form_for(@garrison) do |f| %>
  <% if @garrison.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@garrison.errors.count, "error") %> prohibited this garrison from being saved:</h2>

      <ul>
      <% @garrison.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :reserve_id %><br>
    <%= f.text_field :reserve_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
