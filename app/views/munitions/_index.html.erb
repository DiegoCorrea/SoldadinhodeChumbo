<p id="notice"><%= notice %></p>

<table>
  <thead>
    <tr>
      <th>Caliber</th>
      <th>amount</th>
      <th>Description</th>
      <th colspan="3"></th>
      <th class="text-md-center"><%= link_to new_reserf_garrison_munition_path(@reserve, @garrison) do %><span class="btn btn-success"><i class="fa fa-plus" aria-hidden="true"></i></span><% end %></th>
    </tr>
  </thead>

  <tbody>
    <% @munitions.each do |munition| %>
      <tr>
        <td><%= munition.caliber %></td>
        <td><%= munition.amount %></td>
        <td><%= munition.description %></td>
        <td>
          <%= link_to reserf_garrison_munition_path(@reserve, @garrison, munition) do %><div class="btn btn-info"><i class="fa fa-info" aria-hidden="true"></i></div><% end %>
        </td>
        <td>
          <%= link_to edit_reserf_garrison_munition_path(@reserve, @garrison, munition) do %><div class="btn btn-warning"><i class="fa fa-pencil-square-o" aria-hidden="true"></i></div><% end %>
        </td>
        <td>
          <%= link_to reserf_garrison_munition_path(@reserve, @garrison, munition), method: :delete, data: { confirm: 'Are you sure?' }  do %> <div class="btn btn-danger"><i class="fa fa-times" aria-hidden="true"></i></div><% end %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
  